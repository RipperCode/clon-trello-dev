{
  "name": "trello-clon",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "backend": "json-server --watch db.json"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "vanilla-colorful": "^0.7.2"
  }
}
